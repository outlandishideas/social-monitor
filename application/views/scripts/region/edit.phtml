<?php
/** @var Model_Region $region */
$region = $this->editingRegion;
$presences = $region->getPresences();
$topBar = $this->gatekeeper()->filterAll(array(
	'<a class="button-bc button-view" href="%url%">View Region</a>' => array('controller'=>'region', 'action'=>'view', 'id'=>$region->id),
	'<a class="button-bc button-manage" href="%url%">Manage Countries</a>' => array('controller'=>'region', 'action'=>'manage', 'id'=>$region->id),
    '<a class="button-bc button-delete" href="%url%">Delete</a>' => array('controller'=>'region', 'action'=>'delete', 'id'=>$region->id)
));
$bottomBar = array();
$cancel = $this->gatekeeper()->filter('<a href="%url%" class="button-bc">Cancel</a>', array('controller'=>'region', 'action'=>'view', 'id'=>$region->id), null, true);
if ($cancel) {
    $bottomBar[] = $cancel;
}
$bottomBar[] = '<button type="submit" class="button-bc button-save">Save</button>';
?>

<h2 class="page-title"><?php echo $this->title; ?></h2>

<?php
if ($topBar && $this->showButtons) {
    echo $this->partial('partials/button-bar.phtml', array('buttons'=>$topBar, 'dark'=>true));
}
?>

<form class="edit" method="post" action="<?php echo $this->url(); ?>" novalidate id="edit-region">
    <div class="row">
        <div class="small-24 medium-8 large-8 columns">
            <label for="display_name" class="requiredField">
                Display name<span class="asteriskField">*</span>
            </label>
        </div>
        <div class="small-24 medium-16 large-16 end columns">
            <input name="display_name" value="<?php echo htmlspecialchars($region->display_name); ?>" class="textInput" type="text" id="display_name">
        </div>
    </div>

    <?php if(!empty($presences)) : ?>
    <fieldset class="inlineLabels">
        <h2>Presence details</h2>
        <div class="row">
            <div class="small-24 medium-8 large-8 columns">
                <p>These presences will be automatically added to this region:</p>
            </div>
            <div class="small-24 medium-16 large-16 end columns">
                <ul id="added-presences">
                    <?php
                    foreach ($presences as $presence) {
                        echo $this->partial('partials/presence_box.phtml', array('presence'=>$presence));
                    }
                    ?>
                </ul>
            </div>
        </div>
    </fieldset>
    <?php endif; ?>

    <input name="campaign_type" value="<?php echo Model_Region::$campaignType; ?>" type="hidden" id="campaign_type">

    <?php echo $this->partial('partials/button-bar.phtml', array('buttons'=>$bottomBar, 'dark'=>true)); ?>
</form>

