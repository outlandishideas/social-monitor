<?php
$bottomBar = array();
if ($this->canEdit) {
    $bottomBar = $this->gatekeeper()->filterAll(array(
            '<a class="button-bc" href="%url%">Cancel</a>' => array('controller'=>'index', 'action'=>'index'),
            //todo: reinstate this
//          '<a class="button-bc" href="%url%">Download as CSV</a>' => array('action'=>'download')
        ));
    $bottomBar[] = '<button type="submit" class="button-bc">Save</button>';
}
$topBar = $bottomBar;
array_unshift($topBar, '<div id="search-table">Loading data...</div>');
?>

<h2 class="page-title">Domains</h2>
<div class="row">
    <div class="small-24 medium-12 columns">
        <p>This shows the list of all domains linked to by tweets or facebook statuses. Click the links to see more usage information and the specific URLs.</p>
    </div>
    <div class="small-24 medium-12 columns">
        <p>You can say whether or not a domain belongs to the British Council by toggling the checkbox on the right, and clicking 'save'.
            Note that if you reorder the columns before saving, any changes will be lost.</p>
    </div>
</div>

<form method="post" action="<?php echo $this->url(); ?>">

    <?php echo $this->partial('partials/button-bar.phtml', array('buttons'=>$topBar, 'dark'=>true)); ?>

	<table class="text dtable" id="domains">
		<thead>
			<tr>
				<th data-sort="auto">Domain</th>
				<th data-sort="numeric">Links</th>
				<th data-sort="checkbox">British Council</th>
			</tr>
		</thead>
		<tbody>
		</tbody>
	</table>

    <?php echo $this->partial('partials/button-bar.phtml', array('buttons'=>$bottomBar, 'dark'=>true)); ?>

</form>