<div id="metric-picker-box" class="picker">

	<label for="metric-picker">Metric:</label>
	<select id="metric-picker">
        <?php foreach($this->metricOptions as $key => $value) : ?>
            <option value="<?php echo $key; ?>"><?php echo $value; ?></option>
        <?php endforeach;?>
	</select>

</div>