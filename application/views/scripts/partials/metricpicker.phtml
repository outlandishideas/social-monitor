<div id="metric-nav" class="picker">

	Metric: <select id="metric-picker">
        <?php foreach(Model_Campaign::getKpis(false) as $key => $value) : ?>
            <option value="<?php echo $key; ?>"><?php echo $value; ?></option>
        <?php endforeach;?>
	</select>

</div>