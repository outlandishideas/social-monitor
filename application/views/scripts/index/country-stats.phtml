<div id="country-stats" data-id="<?php echo $this->campaign->id; ?>" data-model="<?php echo $this->model; ?>">
	<div class="close"><a href="#"><span class="icon-remove-circle icon-large"></span></a></div>
	<h3>
		<?php $url = $this->gatekeeper()->filter('%url%', array('action'=>'view', 'controller'=>$this->model, 'id'=>$this->campaign->id)); if ($url) : ?>
        <a href="<?php echo $url; ?>">
            <span class="icon-globe icon-large"></span>
            <?php echo $this->campaign->display_name; ?>
        </a>
		<?php else : ?>
		<span>
			<span class="icon-globe icon-large"></span>
			<?php echo $this->campaign->display_name; ?>
		</span>
		<?php endif; ?>
    </h3>
	<?php if ($this->campaign->presences) : ?>
    <h4><?php echo Model_Badge::badgeTitle($this->metric); ?> (<?php echo $this->date->format('d M Y'); ?>)</h4>
	<ul>
		<?php foreach ($this->campaign->presences as $presence) : /** @var $presence Model_Presence */ ?>

			<?php $metricValue = $this->badgeData[$presence->id][$this->metric]->score; ?>

            <li>
                <span class="icon-circle icon-large traffic-light"
                      style="color: <?php echo $this->trafficLight()->color($metricValue, $this->metric); ?>;">
                </span>
                <?php echo $this->partial('partials/presence_inline.phtml',  array('presence' => $presence)); ?>
	            <?php if ($presence->isForTwitter()) : ?>
		            <span class="klout" title="Klout score: <?php echo $presence->klout_score; ?>"><?php echo round($presence->klout_score); ?></span>
	            <?php endif; ?>
                <span style="float:right;">
                    <span class="<?php echo $presence->sign_off ? 'icon-ok-sign': 'icon-remove-sign';?> icon-large"
                          title="Sign off"></span>
                    <span
                        class="<?php echo $presence->branding ? 'icon-ok-sign': 'icon-remove-sign';?> icon-large"
                        title="Branding Compliance"></span>
                </span>
            </li>

		<?php endforeach; ?>
	</ul>
	<?php else : ?>
		None assigned
	<?php endif; ?>
</div>
