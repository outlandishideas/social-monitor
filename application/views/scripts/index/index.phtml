<ul id="map-tabs" class="content-in-header tabs">
    <?php $first = true; foreach($this->metricOptions as $badge => $title) : ?>
        <li class="<?php if ($first) echo 'active';?>" data-val="<?php echo $badge; ?>"><?php echo ucfirst($title); ?></li>
    <?php $first = false; endforeach; ?>
    <?php if(file_exists(APPLICATION_PATH . '/../data/uploads/kpis.pdf')): ?>
        <li class="fl-right download"><a href="<?php echo $this->url() . 'data/uploads/kpis.pdf'; ?>" target="_blank"><span class="icon-download icon-large"></span> KPIs</a></li>
    <?php endif; ?>
</ul>

<div class="map-date">
    <input type="text" size="25" id="date-slider" disabled value="<?php echo $this->dateRangeString; ?>"
           data-current-date="<?php echo $this->currentDate; ?>" data-day-range="<?php echo $this->dayRange; ?>">
    <div id="slider" data-val="30"></div>
</div>

<div id="map-sidebar" class="">
    <div class="country"></div>
    <div id="instructions-desc" class="desc-box">
        <h3>Instructions <span class="icon-chevron-up"></span></h3>
        <div class="desc-content">
            <p>Choose from either Overall Score, Reach, Engagement or Quality to see the health of each country, SBU and region.</p>
            <p>Move the date slider to view the historic trend of the score for each country, SBU and region.</p>
            <p>Click on the map to select a country and preview the individual social media presences.</p>
            <p>These scores are derived from the organisation’s social media KPIs. You can download a copy by clicking the KPI image on the top right of the screen.</p>
        </div>
    </div><?php
    foreach($this->badgeDescriptions as $badgeTitle => $badgeDesc) : ?>
        <div id="<?php echo $badgeTitle; ?>-desc" class="desc-box hide">
            <h3><?php echo ucfirst($badgeTitle); ?> <span class="icon-chevron-up"></span></h3>
            <div class="desc-content">
                <?php echo $badgeDesc; ?>
            </div>
        </div><?php
    endforeach; ?>

    <div class="loading"></div>
</div>
<div id="map">
    <div id="map-container">
        <div id="geo-map"></div>
    </div>
    <div id="small-maps">
        <h3><?php
            $url = $this->gatekeeper()->filter('%url%', array('controller' => 'country'));
            if($url) : ?>
            <a href="<?php echo $url; ?>">
                <?php endif; ?>
                <span class="<?php echo Model_Country::ICON_TYPE; ?>"></span> Small Countries
                <?php if($url) : ?>
            </a>
        <?php endif; ?>
        </h3>
        <ul></ul>
    </div>
    <div id="group-map">
        <h3><?php
            $url = $this->gatekeeper()->filter('%url%', array('controller' => 'group'));
            if($url) : ?>
            <a href="<?php echo $url; ?>">
                <?php endif; ?>
                <span class="<?php echo Model_Group::ICON_TYPE; ?>"></span> SBUs
                <?php if($url) : ?>
            </a>
            <?php endif; ?>
        </h3>
        <ul></ul>
    </div>
</div>


<script type='text/javascript' src='https://www.google.com/jsapi'></script>
<script type='text/javascript'>
	var mapData = <?php echo json_encode($this->mapData); ?>;
    var smallMapData = <?php echo json_encode($this->smallMapData); ?>;
    var groupData = <?php echo json_encode($this->groupData); ?>;
</script>