<h2 class="page-title">Social Media Monitor</h2>

<div class="row">
    <div class="small-24 medium-8 columns">
        <p>Choose from either Overall, Reach, Engagement or Quality to see the health of each country and SBU.</p>
    </div>
    <div class="small-24 medium-8 columns">
        <p>Move the data slider to view the historic trend of the score for each country and SBU.</p>
    </div>
    <div class="small-24 medium-8 columns">
        <p>Click on a map to select a country and preview the individual social media presences.</p>
    </div>
</div>

<h2 data-badge-title></h2>
<div class="row">
    <div class="small-24 large-6 columns">
        <div class="row">
            <div class="small-12 large-24 columns">
                <?php
                /** @var Badge_Abstract $badge */
                foreach($this->badges as $badge): ?>
                    <div class="badge-description" data-badge-description="<?= $badge->getName(); ?>">
                        <?= $badge->getDescription(); ?>
                    </div>
                <?php endforeach; ?>
            </div>
            <div class="small-12 large-24 columns">
                <h4>Small Countries</h4>
                <ul>
                    <?php
                    /** @var Model_Country $country */
                    foreach($this->smallCountries as $country):
                        $badges = $country->getBadges(); ?>
                        <li><?= $country->getName(); ?> <span data-badge-score></span></li>
                    <?php endforeach; ?>
                </ul>
            </div>
        </div>
    </div>
    <div class="small-24 large-12 columns">
<!--        <div id="map"></div>-->
    </div><?php
    /** @var Model_Country $country */
    $country = $this->countries[0];
    $badges = $country->getBadges();
    $presences = $country->getPresences(); ?>
    <div class="small-24 large-6 columns">
        <div class="badge-small"
             data-total="<?= round($badges['total']); ?>"
             data-reach="<?= round($badges['reach']); ?>"
             data-engagement="<?= round($badges['engagement']); ?>"
             data-quality="<?= round($badges['quality']); ?>">
            <h3><?= $country->display_name; ?></h3>
            <div class="badge-score bd-btm">
                <h4><span data-badge-title></span> Score</h4>
                <div class="score-value" data-badge-score>63%</div>
                <div class="score-bar"><div data-badge-bar></div></div>
            </div>
            <div class="bd-btm">
                <h4>Presences</h4>
                <div class="score-value"><?= count($presences);?></div>
            </div>
            <div class="bd-btm">
                <h4>Twitter</h4>
                <ul class="badge-presences">
                    <?php
                    /** @var NewModel_Presence $presence */
                    foreach($presences as $presence):
                        if($presence->getType() != NewModel_PresenceType::TWITTER) continue; ?>
                        <li>
                            <span class="<?= $presence->getPresenceSign(); ?>"></span> <?= $presence->getName(); ?>
                        </li>
                    <?php endforeach; ?>
                </ul>
            </div>
            <div>
                <h4>Facebook</h4>
                <ul class="badge-presences">
                    <?php
                    /** @var NewModel_Presence $presence */
                    foreach($presences as $presence):
                        if($presence->getType() != NewModel_PresenceType::FACEBOOK) continue; ?>
                        <li>
                            <span class="<?= $presence->getPresenceSign(); ?> fa-lg fa-fw"></span> <?= $presence->getName(); ?>
                        </li>
                    <?php endforeach; ?>
                </ul>
            </div>
        </div>

    </div>
</div>

<div class="button-bc-bar">
    <div class="row">
        <div class="small-24 columns">
            <ul class="inline-list badge-buttons">
                <?php
                /** @var Badge_Abstract $badge */
                foreach($this->badges as $badge): ?>
                    <li><a href="#<?= $badge->getName(); ?>" class="button-bc"><?= $badge->getTitle(); ?></a></li>
                <?php endforeach; ?>
            </ul>
        </div>
    </div>
</div>

<?php if(isset($this->groups) && count($this->groups) > 0): ?>
    <h2>SBUs</h2>
    <div class="row">
        <div class="small-24 large-23 large-centered columns">
            <div class="row">
                <?php
                /** @var Model_Group $group */
                foreach($this->groups as $group):
                    $badges = $group->getBadges(); ?>
                    <div class="small-24 large-8 columns">
                        <div class="badge-small"
                             data-total="<?= round($badges['total']); ?>"
                             data-reach="<?= round($badges['reach']); ?>"
                             data-engagement="<?= round($badges['engagement']); ?>"
                             data-quality="<?= round($badges['quality']); ?>">
                            <h3><?= $group->display_name; ?></h3>
                            <h4><span data-badge-title></span> Score</h4>
                            <div class="score-value" data-badge-score></div>
                            <div class="score-bar">
                                <div data-badge-bar></div>
                            </div>
                            <div class="row">
                                <div class="small-24 large-8 columns">
                                    <h4>Presences</h4>
                                    <div class="score-value">26</div>
                                </div>
                                <div class="small-24 large-8 columns">
                                    <h4>Twitter</h4>
                                    <div class="score-value">13</div>
                                </div>
                                <div class="small-24 large-8 columns">
                                    <h4>Facebook</h4>
                                    <div class="score-value">13</div>
                                </div>
                            </div>
                            <ul class="presence-list">
                                <?php foreach($group->presences as $presence): ?>
                                    <li><span class="<?= $presence->getPresenceSign(); ?>"></span> <?= $presence->getHandle(); ?></li>
                                <?php endforeach; ?>
                            </ul>
                        </div>
                    </div>
                <?php endforeach; ?>
            </div>
        </div>
    </div>
<?php endif; ?>

<div id="badge-titles"
     data-total-title="<?= $this->badges['total']->getTitle(); ?>"
     data-quality-title="<?= $this->badges['quality']->getTitle(); ?>"
     data-reach-title="<?= $this->badges['reach']->getTitle(); ?>"
     data-engagement-title="<?= $this->badges['engagement']->getTitle(); ?>"></div>