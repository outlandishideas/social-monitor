#user:
#  label: User
#  controller: user
#  action: edit-self
#  icon: icon-user icon-large
users:
  label: Users
  controller: user
  icon: icon-group icon-large
  class: parent
  pages:
    account:
      label: Account
      controller: user
      action: edit-self
    edit-all:
      label: Edit Users
      controller: user
    signout:
      label: Sign Out
      controller: user
      action: logout
config:
  label: Settings
  controller: config
  icon: icon-cog icon-large
  class: parent
  pages:
    engagement:
      label: Engagement
      controller: engagement
      action: index
